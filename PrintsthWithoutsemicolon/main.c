#include <stdio.h>

int main(){
    if(printf("Hello world without semicolon with if statement!!!!!\n")){}
    switch(printf("Hello world without semicolon with switch case!!!!!\n")){}
    while(!printf("Hello world without semicolon with while loop!!!!!\n")){}
    return 0;
}
